# Optional: only for a local Supabase stack after enabling Google in config.toml.
# Copy to supabase/.env and never commit the real value.
SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_SECRET=your_google_client_secret
